import 'package:flutter/material.dart';

class ReservationMois extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
        child: Container(
      child: Text("Mois"),
    ));
  }
}
